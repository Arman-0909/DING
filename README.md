# DING

Scaffolded monorepo with backend (FastAPI) and frontend (React + Vite).

## Run backend

```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## Run frontend

```bash
cd frontend
npm install
npm run dev
```
